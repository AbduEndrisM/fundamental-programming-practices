//package inner.anonymous.exercise.functionalinterface;
package four.lambda.functionalinterface;


/**
 *  Define your own functional interface that could be used as a wrapper
 *  for a function that takes zero arguments and returns an integer.
 
 *  Then use it to define an object that produces random numbers.
 *  In a main method test your code;
 *  then try to represent your  functional interface with an anonymous inner class, lambda expression
 *
 * 
 *
 */
@FunctionalInterface
public interface MyIface {

	

}
